🎯 **What:** Extracted the duplicated "Background Effects" HTML layout structure into a reusable Hugo partial at `layouts/partials/background-effects.html`. Then replaced nine identical or nearly-identical HTML background definitions across layout templates and shortcodes with calls to this unified partial.

💡 **Why:** Having multiple disconnected definitions of the same background grid styling (gradient stops, radial opacity overlay) causes significant maintainability debt. Changing the site's thematic background previously required modifying upwards of 9 distinct files simultaneously. Consolidating this to a single partial centralizes UI state and improves code readability.

✅ **Verification:** Verified by injecting the new partial, including testing parameter passing for cases where shortcodes (like `painkiller-pitches`) required a slightly different grid color `#65793e` via a Hugo dictionary parameter structure (`(dict "color" "#65793e")`). Validated build completion using standard Hugo executable processing.

✨ **Result:** Cleaned up ~60 lines of repeated HTML, resulting in easier maintainability for thematic visual assets.
