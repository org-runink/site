node_modules
public
docs

# design-sync (claude.ai/design) — staged converter scripts, generated bundle and
# machine-local state. The durable inputs under .design-sync/ (config.json,
# NOTES.md, conventions.md, previews/) ARE committed.
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules

# @runink/ui build output — regenerated by `npm run build` in the package.
packages/runink-ui/dist/

cmd/models/gemma-3-12b-it-q4_0.gguf
cmd/models/stable-diffusion-v1-5-pruned-emaonly-Q4_1.gguf
# serve.go is tracked; only the local endpoint values stay out of this public
# repo (CI injects the same params from Actions secrets).
serve.env
