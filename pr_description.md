🎯 **What:** Tested `relURL` function in `test-hugo-relurl.go`.
📊 **Coverage:** Covered basic path, empty string, and already absolute paths for `relURL` function.
✨ **Result:** Improved test coverage by verifying the `relURL` function works as intended on multiple scenarios.
