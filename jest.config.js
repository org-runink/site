module.exports = {
  testEnvironment: 'jsdom',
  clearMocks: true,
  setupFiles: ['<rootDir>/jest.setup.js'],
  modulePathIgnorePatterns: ['<rootDir>/themes/'],
  setupFiles: ['<rootDir>/jest.setup.js'],
};
